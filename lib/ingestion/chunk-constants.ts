/** Separate from chunker.ts so client components can show the target range
 *  without pulling the tokenizer into the browser bundle. */
export const MIN_TOKENS = 300;
export const MAX_TOKENS = 500;
