# Ensure Prisma's generated client is resolvable through pnpm's @prisma/client junction
# (needed for IDE TypeScript; CLI tsc often follows realpath and works without this).
public-hoist-pattern[]=*prisma*
public-hoist-pattern[]=*@prisma*
